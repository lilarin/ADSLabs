#include <iostream>
#include <ctime>
#include <cmath>
using namespace std;

int main() {

    int base=4,inputDecimal,rest;
    cin >> inputDecimal;

    while(rest==0) {
        int rest = inputDecimal % base;
    }

    return 1;
}